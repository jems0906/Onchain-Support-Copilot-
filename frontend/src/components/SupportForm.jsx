import { ChevronRight, ShieldAlert } from 'lucide-react';

const tools = ['Foundry', 'Hardhat', 'Coinbase Wallet', 'MetaMask', 'Bridge', 'RPC endpoint', 'SDK'];

export default function SupportForm({ form, update, onSubmit, loading }) {
  return <section className="panel intake"><div className="panel-heading"><div><span className="kicker">01 / BUILDER REPORT</span><h2>Start with the signal</h2></div><span className="required">* Required</span></div><form onSubmit={onSubmit}><div className="field-row"><label>Network<select value={form.network} onChange={(event) => update('network', event.target.value)}><option value="base">Base Mainnet · 8453</option><option value="base-sepolia">Base Sepolia · 84532</option></select></label><label>Tool used<select value={form.tool_used} onChange={(event) => update('tool_used', event.target.value)}>{tools.map((tool) => <option key={tool}>{tool}</option>)}</select></label></div><div className="field-row"><label>Wallet address <span>Optional</span><input value={form.wallet_address} onChange={(event) => update('wallet_address', event.target.value)} placeholder="0x..."/></label><label>Transaction hash <span>Optional</span><input value={form.transaction_hash} onChange={(event) => update('transaction_hash', event.target.value)} placeholder="0x..."/></label></div><label>Contract address <span>Optional</span><input value={form.contract_address} onChange={(event) => update('contract_address', event.target.value)} placeholder="0x..."/></label><label>What happened? <span>Required</span><textarea required minLength="3" value={form.error_message} onChange={(event) => update('error_message', event.target.value)} placeholder="Paste the error message or describe what the builder is seeing..."/></label><div className="form-footer"><span><ShieldAlert size={15}/> Never share private keys or seed phrases</span><button className="primary-btn" disabled={loading}>{loading ? 'Analyzing...' : 'Run diagnosis'} <ChevronRight size={17}/></button></div></form></section>;
}