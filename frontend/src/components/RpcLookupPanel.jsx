export default function RpcLookupPanel({ rpc, onLookup, loading }) {
  return <div className="panel rpc-panel"><div className="panel-heading"><div><span className="kicker">02B / PUBLIC RPC</span><h2>Onchain evidence</h2></div><button className="text-btn" onClick={onLookup} disabled={loading}>{loading ? 'Looking up...' : 'Run lookup'}</button></div>{rpc ? <div className="rpc-results">{rpc.receipt && <div><b>Receipt</b><span>{rpc.receipt.status}{rpc.receipt.found ? ` · ${rpc.receipt.gas_used.toLocaleString()} gas · block ${rpc.receipt.block_number}` : ''}</span></div>}{rpc.balance && <div><b>Balance</b><span>{rpc.balance.eth} ETH</span></div>}{rpc.code && <div><b>Contract</b><span>{rpc.code.deployed ? 'Bytecode deployed' : 'No bytecode found'}</span></div>}</div> : <small className="muted">Add a transaction, wallet, or contract address to inspect public Base data.</small>}</div>;
}